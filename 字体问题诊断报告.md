# 字体问题诊断报告

**日期：** 2026年9月7日

---

## 问题描述

用户报告字体显示不正确：
1. Home页面news部分："6 papers accepted" 等黑色字体不对
2. 从 "实验室在AI for Science..." 之后的黑色字体都不对

---

## 已检查项目

### 1. CSS路径 ✅
**检查结果：** 路径正确但与HTTrack原始文件不同

**HTTrack原始结构：**
```
httrack/bigscity-web/
├── smartcity-buaa.jimdoweb.com/
│   └── index.html  (使用 ../u.jimcdn.com/)
└── u.jimcdn.com/
```

**当前结构：**
```
web/
├── index.html  (使用 u.jimcdn.com/)
└── u.jimcdn.com/
```

**结论：** 路径调整是正确的，适应了新的目录结构。

### 2. CSS文件存在 ✅
- `u.jimcdn.com/cms/o/saf8fe3b8eb300165/layout/lefdf599db517bce3/css/fonta05d.css` ✓
- `fonts.jimstatic.com/cssaa6a` ✓
- `fonts.jimstatic.com/s/ubuntu/v21/4iCs6KVjbNBYlgoKfw7z.ttf` ✓

### 3. CSS加载顺序 ✅
与HTTrack原始文件一致：
1. main CSS
2. layout CSS
3. font CSS
4. web_oldtemplate CSS

---

## 可能的原因

### 1. ChatGPT UI元素影响
在index.html的第182-206行有一个`<article>`块，包含ChatGPT界面的CSS类：
- `text-token-text-primary`
- `user-message-bubble-color`
- `whitespace-pre-wrap`

这些类可能覆盖了默认字体样式。

**位置：** 第182-206行
**内容：** 招聘信息部分（实验室在AI for Science...）

### 2. 内联样式冲突
News部分使用了大量内联样式：
```html
<span style="color: #333333; font-weight: 300;">
```

`font-weight: 300` 可能导致字体看起来更细。

### 3. Ubuntu字体变体
Ubuntu字体有多个变体：
- 400 (regular) - 当前加载的
- 300 (light)
- 700 (bold)

如果原始网站使用了其他变体，但我们只加载了400，字体粗细会不一致。

---

## 建议的解决方案

### 方案1：检查并加载完整的Ubuntu字体家族
当前只加载了weight 400。可能需要：
- Ubuntu 300 (light)
- Ubuntu 500 (medium)
- Ubuntu 700 (bold)

### 方案2：对比HTTrack中实际使用的字体CSS
严格复制HTTrack中fonts.jimstatic.com的全部内容。

### 方案3：检查浏览器渲染
在浏览器开发者工具中：
1. 检查Computed样式中的font-family
2. 检查font-weight实际值
3. 检查是否有其他CSS覆盖

---

## 下一步行动

### 立即测试
```bash
cd F:/bigscity-web/web
python -m http.server 8000
```

访问 http://localhost:8000/ 并：
1. 打开浏览器开发者工具（F12）
2. 选择"6 papers accepted"文本
3. 查看Computed标签中的：
   - font-family
   - font-weight
   - font-size
4. 查看Network标签确认Ubuntu字体是否加载成功

### 对比测试
同时访问HTTrack原始文件：
```bash
cd F:/bigscity-web/httrack/bigscity-web/smartcity-buaa.jimdoweb.com
python -m http.server 8001
```

访问 http://localhost:8001/ 对比字体渲染。

---

## 临时解决方案

如果字体渲染仍然有问题，可以：

1. **强制使用系统字体：**
```css
body, p, span, div {
    font-family: "Microsoft YaHei", "微软雅黑", Arial, sans-serif !important;
}
```

2. **加载Google Fonts（需要网络）：**
```html
<link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500;700&display=swap" rel="stylesheet">
```

3. **使用本地字体备份：**
确保fonts.jimstatic.com中有所有需要的字体变体。

---

## 文件清单

### 已创建的诊断工具
- `check_fonts.py` - 检查字体CSS引用
- `compare_with_original.py` - 对比文件差异
- `font-test.html` - 字体测试页面

### 需要检查的文件
- `index.html` - 第182-206行（ChatGPT UI块）
- `index.html` - 第211-250行（News部分）
- `u.jimcdn.com/cms/o/saf8fe3b8eb300165/layout/lefdf599db517bce3/css/fonta05d.css`
- `fonts.jimstatic.com/cssaa6a`

---

© 2026 BIGSCity Lab  
**字体问题诊断** 🔍  
需要浏览器实际测试确认
